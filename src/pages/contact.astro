---
import BaseLayout from '../layouts/BaseLayout.astro';
import FilAriane from '../components/FilAriane.astro';
import Hero from '../components/Hero.astro';
import Formulaire from '../components/Formulaire.astro';
import Zone from '../components/Zone.astro';
import { siteConfig } from '../config/site.ts';
import monImage from '../images/gants.webp';
import zone from '../images/zone.webp';

const base = import.meta.env.BASE_URL;

const schema = [
  {
    "@context": "https://schema.org",
    "@type": "ContactPage",
    "url": `${siteConfig.url}/contact`
  },
  {
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    "itemListElement": [
      { "@type": "ListItem", "position": 1, "name": "Accueil", "item": siteConfig.url },
      { "@type": "ListItem", "position": 2, "name": "Contact", "item": `${siteConfig.url}/contact` }
    ]
  }
]
---

<BaseLayout
  title={`Contacter ${siteConfig.name}`}
  description={`Contactez ${siteConfig.name} pour toute question sur la boxe thérapie.`}
  canonical={`${siteConfig.url}/contact`}
  ogTitle={`Contacter ${siteConfig.name}`}
  ogDescription={`Contactez ${siteConfig.name} pour toute question sur la boxe thérapie.`}
  schema={schema}
>
  <FilAriane items={[
    { label: "Accueil", href: `${import.meta.env.BASE_URL}` },
    { label: "Contact" }
  ]} />
  
  <Hero 
     titre="Infos pratiques et contact"
    texte={`<h2>Une question, un projet, envie d'en savoir plus ?</h2>
    <p>Écris-moi ou appelle-moi directement. Je réponds à chaque message avec attention.</p>
    📞 Par téléphone : 06 84 55 69 02 <br>
    📧 Par email : nat.boxetherapie@gmail.com <br>
    <p>Sur les réseaux :</p>
<p>
   <a href="https://www.instagram.com/natacha.boxetherapie" target="_blank" rel="noopener">
        <img src="${base}icons/instagram.svg" alt="Instagram" width="22" height="22" style="vertical-align:middle; margin-right:6px;">
        @natacha.boxetherapie
      </a>
    </p>
    <p>
      <a href="https://www.facebook.com/natacha.boxetherapie" target="_blank" rel="noopener">
        <img src="${base}icons/facebook.svg" alt="Facebook" width="22" height="22" style="vertical-align:middle; margin-right:6px;">
        natacha.boxetherapie
      </a>
</p>
    `}
    image={monImage}
    imageAlt="image de Natacha, boxe thérapeute à saumur"
    lienLabel="Découvrez la boxe thérapie"
    lienHref={`${import.meta.env.BASE_URL}`}
  />


  <Formulaire
    action="/traitement-natacha"
    placeholderMessage="Bonjour ! Décrivez ici votre demande..."
  />

  <Zone 
      titre="Ma zone d'intervention"
      image={zone}
      imageAlt="carte de la zone d'intervention de Natacha Boxe thérapie à Saumur"
      carteUrl="mettre le lien google maps"
  />

</BaseLayout>